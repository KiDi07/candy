# Candy - Telegram Bot for Selling Recipes

Современный и масштабируемый Telegram бот для продажи цифрового контента (рецептов) с защитой от копирования. Построен на базе **aiogram 3.x** и асинхронной **SQLAlchemy 2.0**.

## 🚀 Основные возможности
- **Каталог рецептов**: Динамический список с проверкой прав доступа.
- **Система покупок**: Автоматическое отслеживание купленных курсов через БД.
- **Защита контента**: Использование `protect_content=True` для предотвращения пересылки сообщений и скриншотов.
- **Асинхронность**: Полностью асинхронная работа с API и базой данных (SQLite + aiosqlite).
- **Чистая архитектура**: Использование роутеров, мидлварей и современной структуры папок.

## 📂 Структура проекта
- `run.py` — точка входа, инициализация бота и БД.
- `bot/`
  - `config/` — управление конфигурацией и переменными окружения.
  - `database/` — модели SQLAlchemy и асинхронное подключение.
  - `handlers/` — логика обработки команд и callback-запросов.
  - `keyboards/` — генерация инлайн-клавиатур.
  - `middlewares/` — мидлварь для проброса сессии БД в хэндлеры.
  - `utils/` — вспомогательные функции.

## 🛠 Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/KiDi07/candy.git
   cd candy
   ```

2. **Создайте и активируйте виртуальное окружение:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # для macOS/Linux
   # или
   venv\Scripts\activate     # для Windows
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения:**
   Создайте файл `.env` в корневой папке:
   ```env
   BOT_TOKEN=ваш_токен_от_BotFather
   ```

5. **Запустите бота:**
   ```bash
   python run.py
   ```

## 📝 База данных
При первом запуске автоматически создается файл `db.sqlite3` и необходимые таблицы. Также добавляется один тестовый рецепт для проверки функционала.

## 🔒 Безопасность
Бот использует встроенную защиту Telegram для платного контента. Сообщения с рецептами нельзя переслать, а на большинстве устройств невозможно сделать их скриншот или запись экрана.
